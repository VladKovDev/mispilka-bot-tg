database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  name: promo_bots
  sslmode: require
  max_open_conns: 10
  max_idle_conns: 5

logger:
  level: info
  format: json
  output: stdout
  enable_colors: true